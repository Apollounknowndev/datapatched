{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "Apollo"
  ],
  "contact": {
    "homepage": "${url}",
    "sources": "${sources}"
  },
  "license": "MIT",
  "icon": "pack.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.worldgen.datapatched.impl.platform.fabric.DatapatchedFabric"
    ]
  },
  "mixins": [
    "datapatched.mixins.json"
  ],
  "accessWidener": "datapatched.ct",
  "depends": {
    "fabricloader": ">=0.17",
    "minecraft": "${minecraft}"
  }
}